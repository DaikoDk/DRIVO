meta {
  name: 06 Desactivar Marca
  type: http
  seq: 6
}

delete {
  url: http://localhost:8080/api/marcas/2
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
