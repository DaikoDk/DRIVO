meta {
  name: 08 Desactivar Cliente
  type: http
  seq: 8
}

delete {
  url: http://localhost:8080/api/clientes/3
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
