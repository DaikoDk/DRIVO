meta {
  name: 06 Filtrar Por Estado
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/api/reparaciones?estado=Completada
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
