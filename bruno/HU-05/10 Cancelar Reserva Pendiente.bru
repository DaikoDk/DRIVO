meta {
  name: 08 Cancelar Reserva Pendiente
  type: http
  seq: 10
}

patch {
  url: http://localhost:8080/api/reservas/1011/cancelar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.estado: eq Cancelada
}
