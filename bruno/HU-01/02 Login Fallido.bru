meta {
  name: 02 Login Fallido
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "correo": "admin@drivo.com",
    "clave": "passwordIncorrecta"
  }
}

assert {
  res.status: eq 401
}
