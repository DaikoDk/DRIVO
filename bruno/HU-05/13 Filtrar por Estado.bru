meta {
  name: 11 Filtrar por Estado
  type: http
  seq: 13
}

get {
  url: http://localhost:8080/api/reservas?estado=Pendiente
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
